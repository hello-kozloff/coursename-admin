import styled from 'styled-components'

export const Logo = styled.a`
  color: #000000;
  font-weight: bold;
  font-size: 18px;
  transition: .25s ease-out;
  
  &:hover, &:focus-visible {
    color: #2DB38B;
  }
`
